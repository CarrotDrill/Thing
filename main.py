import pygame

my_list = []

for e in range(10): 
  print ("The count is: " + str(e+1))
  my_list.append(e+1)

# Write a program that uses a loop fill an empty list with 10
#numbers and prints the list to the console.
